# ============================================
# ENVIRONMENT CONFIGURATION - EXAMPLE
# ============================================
#
# ⚠️ DO NOT USE THIS FILE DIRECTLY
#
# This is an example file showing the structure.
#
# For actual configuration, use one of these:
#
# DEVELOPMENT:
#   cp config/env/.env.development .env
#
# PRODUCTION:
#   cp config/env/.env.production .env
#
# See config/README.md for detailed instructions
# ============================================

# Database
POSTGRES_DB=your_database_name
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your_password_here
DATABASE_URL=postgresql://postgres:your_password_here@localhost:5432/your_database_name

# Backend
NODE_ENV=development
BACKEND_PORT=3001
API_KEY=your_api_key_here
FRONTEND_URL=http://localhost:3000

# Frontend
VITE_API_URL=http://localhost:3001/api
VITE_API_KEY=your_api_key_here

# Docker Ports
POSTGRES_HOST_PORT=5432
BACKEND_HOST_PORT=3001
FRONTEND_HOST_PORT=3000

# ============================================
# QUICK START:
# ============================================
# 1. Choose your environment:
#    cp config/env/.env.development .env     (for dev)
#    cp config/env/.env.production .env      (for prod)
#
# 2. Update the values in .env
#
# 3. Start application:
#    docker-compose -f docker-compose.dev.yml up     (dev)
#    docker-compose -f docker-compose.prod.yml up    (prod)
# ============================================
